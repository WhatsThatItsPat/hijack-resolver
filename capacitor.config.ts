import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hijack-resolver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
